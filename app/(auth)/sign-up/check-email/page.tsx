import CheckEmail from "components/auth/checkEmail";

const EmailFormCom = async () => {
  return (
    <section className="flex-center size-full max-sm:px-6">
      <CheckEmail />
    </section>
  );
};

export default EmailFormCom;
